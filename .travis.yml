language: ruby
rvm:
  - 2.3.0
env:
  - DB=postgresql
script:
  - bundle exec rake db:migrate
  - bundle exec rake spec
before_install:
  - cp config/database.yml.postgres.sample.yaml config/database.yml
  - psql -c 'create database arenah_test' -U postgres
