<%= content_tag :div, class: 'attributes-group', data: group.to_params do %>
  <%= render 'game/sheet/attributes_groups/group_title', group: group %>

  <table class="table table-condensed name-value-attributes">
    <tbody>
      <% group.equipments.each do |equipment| %>
        <%= render 'game/sheet/attributes_types/equipment', equipment: equipment %>
      <% end %>

      <%= render 'game/sheet/attributes_groups/empty_group', text: 'Sem equipamentos' if group.equipments.count == 0 %>
    </tbody>
  </table>
<% end %>
