<%
  game_slug = @game.try(:slug) || ''
  topic_slug = @topic.try(:slug) || ''
  character_slug = @character.try(:slug) || ''
%>

<%= hidden_field 'game-room', nil, value: game_slug %>
<%= hidden_field 'environment', nil, value: Rails.env %>
<%= hidden_field 'topic-slug', nil, value: topic_slug %>
<%= hidden_field 'character-slug', nil, value: character_slug %>
